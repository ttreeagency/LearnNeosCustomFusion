prototype(Ttree.LearnNeosCustomFusion:WeatherToday) {
    @class = 'Ttree\\LearnNeosCustomFusion\\Fusion\\WeatherTodayImplementation'

    today = ${Date.format(this.data.today, 'D d')}

    @cache {
        mode = 'cached'

        maximumLifetime = Ttree.UntilMidnight:Seconds
    }
}
